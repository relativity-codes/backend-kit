'use strict';

/** @type {import('sequelize-cli').Migration} */
module.exports = {
  // The `up` function creates the table with the correct foreign key
  async up(queryInterface, Sequelize) {
  
  },

  // The `down` function reverts the changes, dropping the table
  async down(queryInterface, Sequelize) {
  }
};
